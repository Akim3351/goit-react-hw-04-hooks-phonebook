import styled from '@emotion/styled';

export const ContactList = styled.ul`
  padding: 10px;
  max-width: 600px;
  list-style: none;
  display: flex;
  flex-direction: column;
  text-align: left;
`;
